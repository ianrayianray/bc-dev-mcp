node_modules/
dist/
*.exe

# Session scratch space — experiments, logs, captured profiles. Never ship.
scratch/

# Internal working docs (Superpowers specs/plans) — local-only, not part of the public repo.
docs/superpowers/

# AL compiler symbol cache and compiled packages — large, environment-specific, and
# easy to re-fetch/rebuild (see demos/hello-bug, demos/type-zoo, and README for how).
demos/*/.alpackages/
*.app
reference/
CLAUDE.local.md

# session scratch (SDD ledgers, briefs)
.superpowers/
